services:
  app:
    build:
      context: .
      target: runner
      args:
        VITE_BASE_URL: ${VITE_BASE_URL:-http://localhost:3000}
    container_name: syncnotes-frontend-prod
    restart: unless-stopped
    ports:
      - '${FRONTEND_PORT:-80}:80'
